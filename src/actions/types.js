export const SIGN_IN = "SIGN_IN";
export const SIGN_OUT = "SIGN_OUT";

// Assign action types string values to variables and use them instead to prevent typos
// In case of typo see errors instead of just nothing happening is state
